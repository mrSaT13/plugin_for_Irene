### 🎵 Плагин «Music Assistant» для Irene (VACore)
Позволяет управлять Music Assistant через голосовые команды:

«пауза», «продолжи», «следующий трек».

💡 Требуется запущенный сервер Music Assistant с включённым HTTP API и настроенным API-токеном.

### 🗣️ Как использовать?
«пауза» → остановит воспроизведение

«продолжи» → возобновит музыку

«следующий трек» → переключит на следующую композицию

### ⚙️ Настройка (в файле плагина)
Файл конфигурации через веб-интерфейс:

```json
{
  "ma_url": "http://music-assistant:8095",
  "api_token": "ваш_токен_из_Music_Assistant",
  "player_id": "id_вашего_плеера"
}



ma_url — адрес сервера Music Assistant (обычно http://music-assistant:8095 в Docker)

api_token — создаётся в интерфейсе Music Assistant → Settings → Advanced → API Tokens

player_id — ID плеера (можно найти в логах или через API)

```
### 🔒 НИКОГДА не коммитьте api_token в Git! Заполняйте конфиг только локально.

### 📦 Установка

Скопируйте файл music_assistant.py в папку plugins/ вашего ассистента Irene.

Перезапустите Irene.

### 📝 Требования
Работает с Irene Voice Assistant (форк от AlexeyBond).

Python 3.7+

Зависимости: requests (обычно уже установлен)
### 👤 Автор
mrSaT13
